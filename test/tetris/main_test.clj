;(ns tetris.main-test
;  (:require [tetris.main :as m]
;            [clojure.test :as t]))
;
;
;;; TODO: fix it
;(t/deftest move-piece
;    (t/testing "move-piece"
;      (t/is (= false (m/move-piece [])))))
;
